# Cross-Border Seller Radar

Cloud pilot bootstrap for the official-source change → seller-SKU review workflow.

The existing `hourei-proxy` app is left untouched. This branch only establishes a documented integration boundary so the pilot can be promoted into a dedicated application/repository after validation.

See `ARCHITECTURE.md` for the current hard gates and security boundary.
